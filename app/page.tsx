export default function DashboardPage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2 p-6 text-center">
      <h1 className="text-[length:var(--text-display)] font-bold leading-[var(--text-display--line-height)]">
        WEEX Alpha Desk
      </h1>
      <p className="text-[length:var(--text-body)] text-[color:var(--color-text-secondary)]">
        Scaffold online. Strategy engines, guardrails, and dashboard modules
        load next.
      </p>
    </main>
  );
}
